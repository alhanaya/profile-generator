# Profile Generator (Python)

A small Python tool that generates a professional GitHub profile README from structured data (`profile.json`).

## How it works
- Edit `profile.json`
- Run `generate.py`
- Get the generated output in `dist/README.md`

## Setup
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
